<Window x:Class="ExamSocket.Views.SignupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExamSocket.Views"
        mc:Ignorable="d"
        Title="회원가입" Height="450" Width="360">
    <Border Background="white" CornerRadius="15" Margin="20" Padding="10">
        <Border.Effect>
            <DropShadowEffect Color="#DDDDDD" BlurRadius="20" ShadowDepth="5" Opacity="0.5"/>
        </Border.Effect>
        
        <Grid VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="회원가입" FontSize="24" FontWeight="Bold" 
                       HorizontalAlignment="Center" Foreground="#333"/>

            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="아이디" Foreground="#555" Margin="2,0,0,5"/>
                <TextBox Grid.Row="1" Text="{Binding Id}" 
                         Height="38" VerticalContentAlignment="Center" Padding="8"/>
            </Grid>

            <Grid Grid.Row="4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="비밀번호" Foreground="#555" Margin="2,0,0,5"/>
                <PasswordBox Grid.Row="1" Name="SignupPwBox" PasswordChanged="SignupPwBox_PasswordChanged"
                             Height="38" VerticalContentAlignment="Center" Padding="8"/>
            </Grid>

            <Grid Grid.Row="6">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="이름" Foreground="#555" Margin="2,0,0,5"/>
                <TextBox Grid.Row="1" Text="{Binding UserName}" 
                         Height="38" VerticalContentAlignment="Center" Padding="8"/>
            </Grid>

            <Grid Grid.Row="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="취소" Command="{Binding CloseCommand}"
                        Height="45" Background="#E0E0E0" Foreground="#555"
                        FontSize="14" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button Grid.Column="2" Content="가입하기" Command="{Binding RegisterCommand}"
                        Height="45" Background="#007ACC" Foreground="White" 
                        FontSize="14" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>
